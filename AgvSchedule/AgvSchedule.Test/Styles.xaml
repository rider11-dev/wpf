<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--<Style x:Key="userCtlStyle" TargetType="UserControl">
        <ControlTemplate>
            
        </ControlTemplate>
    </Style>-->
    <ControlTemplate x:Key="userCtlTemplate" TargetType="UserControl">
        <Grid ShowGridLines="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="基于路径的动画"/>
            <Canvas Grid.Row="1">
                <ContentControl/>
            </Canvas>
        </Grid>
    </ControlTemplate>
</ResourceDictionary>